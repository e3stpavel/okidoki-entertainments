import { StyleSheet, Text } from 'react-native'

const styles = StyleSheet.create({
  t: {
    color: 'blue',
  },
})

const Comp = () => (
  <Text style={ styles.t }>UUY</Text>
)

export default Comp
